(()=>{const root=document.querySelector('[data-zed-home]');if(!root)return;const reduce=matchMedia('(prefers-reduced-motion: reduce)').matches;const thread=root.querySelector('[data-zed-thread-path]');if(thread){const len=thread.getTotalLength();thread.style.strokeDasharray=len;thread.style.strokeDashoffset=len;const draw=()=>{const max=document.documentElement.scrollHeight-innerHeight;const p=max>0?scrollY/max:0;thread.style.strokeDashoffset=String(len*(1-Math.min(1,p*1.35)))};draw();addEventListener('scroll',draw,{passive:true})}
const reveal=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting)e.target.classList.add('is-visible')}),{threshold:.16});root.querySelectorAll('[data-zed-reveal]').forEach(el=>{el.classList.add('zed-reveal');reveal.observe(el)});
if(!reduce){const tilt=root.querySelector('[data-zed-tilt]');if(tilt){tilt.addEventListener('pointermove',e=>{const r=tilt.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;tilt.style.transform=`perspective(1000px) rotateY(${x*8}deg) rotateX(${-y*7}deg) translate3d(0,${y*8}px,0)`});tilt.addEventListener('pointerleave',()=>tilt.style.transform='')}
const detail=root.querySelector('[data-zed-detail]');const lens=root.querySelector('.zed-lens');const image=root.querySelector('.zed-detail-stage__visual img');const horizontal=root.querySelector('[data-zed-horizontal]');const track=root.querySelector('.zed-horizontal__track');const animate=()=>{if(detail&&image&&lens){const r=detail.getBoundingClientRect(),span=Math.max(1,detail.offsetHeight-innerHeight),p=Math.min(1,Math.max(0,-r.top/span));image.style.transform=`scale(${1.16+p*.34}) translate3d(${(p-.5)*-4}%,${(p-.5)*-7}%,0)`;lens.style.left=`${28+p*48}%`;lens.style.top=`${64-p*37}%`}if(horizontal&&track&&innerWidth>800){const r=horizontal.getBoundingClientRect(),span=Math.max(1,horizontal.offsetHeight-innerHeight),p=Math.min(1,Math.max(0,-r.top/span));const max=Math.max(0,track.scrollWidth-innerWidth);track.style.transform=`translate3d(${-max*p}px,0,0)`}requestAnimationFrame(animate)};requestAnimationFrame(animate)}})();
